# 連線設定範本
# 使用方式：複製此檔為 .env 並填入實際值（.env 不會進入 git 版控）

# --- PostgreSQL ---
PGHOST=localhost
PGPORT=5432
PGDATABASE=
PGUSER=
PGPASSWORD=

# --- MongoDB ---
# 本機 mongod 預設無認證，URI 直接指定 host:port 即可
MONGO_URI=mongodb://localhost:27017
MONGO_DATABASE=
